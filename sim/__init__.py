"""Rynmesh credit-economy simulation harness.

Run as ``python -m sim.run`` from the repo root, or import individual
scenarios for ad-hoc analysis. The harness models peers, identity tiers,
credit events, and adversarial strategies against the production
``CreditLedger`` + ``identity`` primitives so design changes can be
evaluated quantitatively before they ship.
"""
