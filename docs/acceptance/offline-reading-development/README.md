# 离线阅读开发验证

日期：2026-09-11。当前为后端开发增量，尚未完整通过 OFFLINE01–10 和通用验收。

## 已接入

- 新增独立 `offline_reading` 路由包，复用节点 Owner 鉴权、后台任务注册表、原子写入、节点密钥加密和现有正文提取模块。
- 普通收藏不会创建离线副本。明确下载后生成一个任务，重复请求复用活动任务；正文未提交前，读取接口返回未下载。
- 状态区分排队、下载、校验、可用、部分可用、失败、取消请求、已取消和已清理。记录经过校验的字节，不虚构百分比或 HTTP 字节续传。
- 正文和每张已处理图片分别写入加密检查点。重启只复用能重新认证的检查点；任务身份和执行令牌阻止取消、清理或重启之前的迟到结果写回。
- 更新由用户明确请求；新副本提交前保留旧正文、原下载时间和图片版本。图片接口绑定副本版本，更新后旧图片 URL 返回版本变化。
- 单项加密文件限制 16 MiB，总预算 256 MiB，其中为元数据保留 4 MiB。网页输入最多 8 MiB，正文最多 4 MiB，图片最多 8 张、每张 2 MiB；图片尺寸及像素数量也有上限。原子替换计入旧文件仍占用时的峰值空间。
- 单条和全部清理先返回副本数、暂存任务数、大小及确认标识；期间任务变化会使旧确认失效。清理限定在离线包，保留收藏、阅读进度和独立导入副本；取消收藏也不会自动清理下载。
- 公网页面重新获取正文，不把有时效的阅读缓存当成明确下载。来源和图片地址逐次验证，阻止内网、凭证 URL、主动脚本和 SVG；固定已校验地址连接，保留 TLS 主机校验，不发送节点凭证、Cookie 或 Referer。
- 网络下载由一个受监督子进程执行，父进程限制 DNS、连接、响应头和正文的总时限，并能取消、终止和回收。仅开发验收可通过 `RYNMESH_OFFLINE_ALLOW_LOOPBACK=1` 允许回环来源，默认拒绝。
- 已导入的好友文档作为接收者的独立本地副本读取，先使用现有导入模块验证内容。此入口不向原发布者请求新版本。
- 网络和存储失败既记入任务安全错误码，也交给后台注册表统计；损坏离线数据不阻止节点应用创建。
- 清理或替换前检查关联文件版本，未知版本拒绝并保留元数据引用；无引用的未来版本文件也不由当前代码删除。已清理的记录可让出元数据名额，不会形成永久的下载次数限制。

## 自动验证

运行环境：Windows、Python 虚拟环境、真实临时文件、真实节点密钥和加密、真实回环 HTTP 服务。网络内容均为合成文章与图片，无真实用户文档或模型。

`tests/test_offline_reading.py` 覆盖：

- 收藏不等于下载，重复排队只有一个结果，重建服务后不联网读正文和图片。
- 图片失败保留正文并标记部分可用，更新失败保留旧版，更新成功再替换。
- 下载期间取消、清理和重装后的迟到响应被拒绝；恢复只获取尚未完成的资源。
- 真正终止下载进程，再从磁盘检查点恢复；关闭正文来源后仍能继续完成图片和提交正文。
- 单项、总量和磁盘空间不足不驱逐旧版；确认内容变化后拒绝过期清理，收藏/进度和独立副本保持。
- 加密内容不出现明文正文或来源 URL；损坏正文拒绝读取，损坏暂存可重建，未知文件版本拒绝覆盖，元数据扩展字段保留。
- Owner 拒绝、请求体限长、路由和工作器重装、图片版本绑定、主动资源拒绝。
- 实际 HTTP 重定向、内网目标拒绝、401/403 类访问限制中的 403 实测、传输长度不符及大小限制。
- 服务器持续发送但不结束响应头时，时限和取消仍生效，子进程回收。

- 后端全量：1099 passed / 29 skipped / 5 warnings，204.88 秒。
- 随后补上未来版本清理预检、未知孤立文件保留和已清理名额复用；最终离线、阅读器、后台工作器回归：77 passed / 1 warning，14.30 秒。未由新增专项数量推算新的全量结果。
- Ruff 和验收清单一致性检查通过。警告来自现有 Starlette TestClient 与 FastAPI 生命周期弃用提示。

## 待完成的产品验收

- 收藏/阅读页下载入口、下载列表和空间确认界面尚未接入；当前只有节点接口，不作为页面闭环完成。
- 搜索尚未接入离线正文，也未验证清理后索引移除；阅读位置由现有消费记录模块保存，离线页面尚未验证。
- 当前网页来源支持 HTML、纯文本和 Markdown；网页 PDF、登录后内容、完整视频和音频不支持。登录页返回 200 时仍需要更全面的无正文识别验证。
- 图片目前验证受限格式、尺寸、字节哈希及加密完整性；尚无全格式解码验收，页面需要显示解码失败。图片达到数量上限或文档已截断时必须说明部分可用。
- 从好友获取新版本的授权链尚未接入此下载入口；已有独立副本可读不等于完成撤权后新版本拒绝的完整产品验收。
- 新数据的完整导出/擦除、异常文件清理、打包桌面、键盘和焦点操作尚待验证。已为 PyInstaller 入口补上多进程初始化，但未声称打包平台已运行。

所有相关用例保持开发中或未执行；进程测试和回环 HTTP 测试均不能替代打包桌面的断网阅读验收。
