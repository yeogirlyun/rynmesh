# 搜索与找回开发验证

日期：2026-09-11。当前只有检索核心，尚未接入四类真实数据、HTTP 接口或页面，不代表搜索功能已交付。

## 已实现

- 本地关键词匹配：空白分隔的关键词取交集；中文无空格词组直接匹配，英文不区分大小写。标题匹配优先，也可按时间排序。
- 支持类型、来源、好友及时间范围筛选；默认每页 20 条。内容身份由数据适配器提供，相同文本的不同消息保留独立结果。
- 匹配片段返回纯文本和 Unicode 码点范围，页面应按码点切片高亮，不能当 HTML 渲染。
- 查询时重新读取授权数据，核对与索引记录的指纹。删除、撤销或修改后的旧正文不返回；索引尚未更新时标记部分结果。数据源读取失败时返回固定错误码。
- 索引按节点密钥加密，使用已有原子存储和文件事务。缓存损坏可从原数据重建；未知版本拒绝覆盖；未知扩展字段保留。
- 重建状态可查询，同时只允许一个重建。分页签名绑定查询、筛选和结果版本，发生变化要求刷新，避免静默重复或跳过记录。
- 每个索引最多 10 万条、64 MiB 明文；倒排表超过计数限制时回退为完整候选扫描，避免无限增加内存。超出数据上限明确失败，不修改原数据。

## 自动验证

`tests/test_local_search.py` 使用真实临时文件、真实加密和重启对象，数据来源目前是合成记录列表。

- 首轮 8 passed，涵盖中文/英文/混合查询、身份与类型、过滤、权限删除、加密存储、损坏恢复、并发重建和分页。
- 随后补齐 emoji 前后的高亮偏移与非法筛选拒绝，8 passed / 1 deselected；未重复运行未变动的性能用例。Ruff 通过。
- [核心性能记录](core-performance.json)：本机 Windows，1 万条合成记录，冷建索引 0.938 秒，20 次查询首屏 p95 0.385 秒。包含当前合成数据校验，不包含四类真实文件读取、HTTP 或页面耗时。pytest 输出含一条 JUnit 属性格式提示，不影响测试结果。

## 后续验收步骤

1. 接入收藏/历史、已收到的好友分享、好友聊天和 Ask Ryn，使用四类不同合成关键词验证定位入口。
2. 确认搜索不触发正文获取或任何外部请求。验证撤权后旧索引不泄露片段，已明确保存的独立副本保留。
3. 接入统一搜索入口、筛选、高亮、返回位置恢复、请求竞态保护及索引重建界面。
4. 在真实存储和页面上重复新增/编辑/删除、断网、损坏重启以及 1 万条性能测量，再执行对应产品验收。

SEARCH01–11 均未完整通过。
