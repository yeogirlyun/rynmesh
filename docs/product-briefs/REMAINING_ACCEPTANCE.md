# 剩余验收与停止条件

2026-09-14。原始八项需求、95 条用例不变。最新逐项复核为 **82 条在本轮范围内通过，13 条继续核对**，详见[完整结论及逐项证据](../acceptance/candidate-delivery/requirement-review-20260914.md)。这是范围内的用例结果，不是整体发布完成率或概率承诺。

macOS 与公网已获用户明确同意跳过，见[范围确认](ACCEPTANCE_SCOPE.md)。不等待这两类环境，不把未测写成通过。当前试用为 Windows 安装 wheel 与浏览器；双节点分别使用独立身份和数据目录。

| 剩余事项 | 原用例 | 明确收尾动作 |
|---|---|---|
| 私有内容和秘密的统一边界 | READ10、SHARE10、ASK10、SEARCH11、G06 | 沿实际日志、诊断、公开注册、模型与外发入口核对字段；复用已完成的导出文件、密钥排除和权限拒绝证据，补确切缺口 |
| 数据范围与同步清空 | G08、SYNC13 | 把各新增数据的导出、擦除、必要保留范围与升级证据逐项对齐；核对同步清空中冲突/恢复项及远端未确认提示 |
| 通用入口、状态和键盘 | G01、G02、G03、G07 | 汇总八入口的空数据/主流程/状态/键盘证据，只补未覆盖的主要操作；设置首次加载失败的永久转圈已修复并通过安装包页面重试 |
| 原生运行时依赖缺失 | AI04 | 在支持的 Windows CPU 配置核对依赖缺失及修复后的就绪状态；已有模型/可执行文件移走和恢复、校验失败不重复验 |
| 远端产品 CI | SHARE11 | 本地两节点及配额命令已通过；候选 GitHub Actions 尚未执行，不用本地结果冒充。公网部分已跳过 |

现有环境中没有因为缺少 macOS 或公网而停下的本机任务。候选代码 `858cda5`，本轮前端 252 项通过、类型检查与构建通过；后端产品代码不变，复用 1482 passed / 29 skipped 的完整结果。新增定向检查独立列出，不累加冒充全量次数。

执行与停止规则：

1. 只围绕以上原用例缺口收尾，不自动扩展图片格式、登录系统、屏幕尺寸、更多硬件或更多网络组合。
2. 每次新检查对应一个原用例和明确未证实条件；较新的证据已经补足的历史“待验”不再重复运行。
3. 单个功能必须有实际页面主流程；分支使用真实存储/加密/HTTP 与组件检查共同验证，并记录模拟与实测边界。
4. 发现影响使用的缺陷先复现、作必要修复，回归受影响范围。完成检查后更新可安装候选，避免只留下源代码修复。
5. 全部必测项满足后才能宣布目标完成。权限、数据和失败恢复的真实缺口不能因耗时长或大部分用例通过而跳过。

`origin/main` 已同步至已审查的 `5ab71ab`；候选未合并或发布。最新运行入口见[试用说明](TRY_CANDIDATE.md)。历史失败和测量记录保留。
