# 剩余验收与停止条件

2026-09-14。原始八项需求、95 条用例不变。最新逐项复核为 **92 条在本轮范围内通过，3 条继续核对**，详见[完整结论及逐项证据](../acceptance/candidate-delivery/requirement-review-20260914.md)。这是范围内的用例结果，不是整体发布完成率或概率承诺。

macOS 与公网已获用户明确同意跳过，见[范围确认](ACCEPTANCE_SCOPE.md)。不等待这两类环境，不把未测写成通过。当前试用为 Windows 安装 wheel 与浏览器；双节点分别使用独立身份和数据目录。

| 剩余事项 | 原用例 | 明确收尾动作 |
|---|---|---|
| 数据范围 | G08 | 已确认好友卡片历史可导出，但缺少节点本机的清理入口；移除好友只撤权，不能冒充擦除。补此范围并明确关系/授权防重放记录的保留理由；已通过的会话、阅读、文档、动态和离线清理不重复验 |
| 主要流程键盘 | G07 | 补未证实的主要键盘操作；八入口空安装和状态已通过 G01/G03，主流程已通过 G02，不重复运行 |
| 远端产品 CI | SHARE11 | 本地两节点及配额命令已通过；候选 GitHub Actions 尚未执行，不用本地结果冒充。公网部分已跳过 |

现有环境中没有因为缺少 macOS 或公网而停下的本机任务。候选代码 `f486fb7`，修复三个日志问题及运行时依赖无法修复的问题。Windows 真实 DLL 缺失后，经产品 Update runtime 重新下载修复并通过自检。最终相关后端 138 passed / 17 skipped，AI 页面 34 passed，类型检查、构建与 Ruff 通过；前端其余功能沿用此前 252 项全量结果。本轮中途的全后端结果不作为最终提交全量证明，不累加测试次数。

执行与停止规则：

1. 只围绕以上原用例缺口收尾，不自动扩展图片格式、登录系统、屏幕尺寸、更多硬件或更多网络组合。
2. 每次新检查对应一个原用例和明确未证实条件；较新的证据已经补足的历史“待验”不再重复运行。
3. 单个功能必须有实际页面主流程；分支使用真实存储/加密/HTTP 与组件检查共同验证，并记录模拟与实测边界。
4. 发现影响使用的缺陷先复现、作必要修复，回归受影响范围。完成检查后更新可安装候选，避免只留下源代码修复。
5. 全部必测项满足后才能宣布目标完成。权限、数据和失败恢复的真实缺口不能因耗时长或大部分用例通过而跳过。

`origin/main` 已同步至已审查的 `5ab71ab`；候选未合并或发布。最新运行入口见[试用说明](TRY_CANDIDATE.md)。历史失败和测量记录保留。
